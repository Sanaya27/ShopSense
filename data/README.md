This project uses the Amazon ESCI dataset.

Download:
- shopping_queries_dataset_examples.parquet
- shopping_queries_dataset_products.parquet

Place them inside the data/ folder before running the pipeline.